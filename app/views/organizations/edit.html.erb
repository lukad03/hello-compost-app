<header>
<h1><%= @organization.name %></h1>
</header>

<%= simple_form_for @organization do |organization| %>
  <%= organization.input :name, label: 'Name:' %>
  <%= organization.button :submit, value: "Update Organization", class: "primary-button" %>
<% end %>
